---
name: git-commit
description: Правила этапа Commit + Push. Используй когда пользователь просит сделать commit или commit + push.
---

# Git-ассистент и наставничество

Роль: не только Git-наставник, но и Git-ассистент.
Ассистент обязан не только объяснять, но и выполнять Git-действия по правилам проекта.

## Запуск этапа
- `автокоммит = нет`: Commit + Push запускается только по явной команде пользователя
- Перед коммитом покажи пользователю текст коммита (заголовок и тело) и дождись явного подтверждения
- **Один коммит = одна цельная единица** (концепция/цикл). Размер идёт от единицы, не от лимита строк: маленький фикс — маленький коммит, большой цельный цикл — большой, и это нормально. Резать нужно только когда в один коммит попали **несвязанные** дела (если заголовок требует «+»/«и» из разных областей — разнести).
- Перед коммитом проверь `git status` и исключи случайные/временные файлы
- Не коммить секреты, `.env`, рабочие данные и прочие артефакты среды

## Ветки
- **Не создавай ветку по умолчанию** — коммить в текущую ветку.
- Если проект работает trunk-based (вся история в `master`/`main`) — коммить прямо туда.
- Отдельную ветку заводи только по явной просьбе пользователя.

## Push-политика
- Commit и Push можно запросить вместе
- Если запрошены оба действия, после подтверждения текста выполни commit, затем push без второго запроса разрешения
- Если запрошен только Commit, не выполняй Push
- Перед push проверь `git remote -v` и отправь текущую ветку во все настроенные remote
- Не определяй хостинг по имени remote: `origin` может вести на GitHub, GitLab или другой сервер
- Если настроены remote и для GitHub, и для GitLab, push должен уйти в оба
- Если локальные коммиты долго не отправлены, явно предупреди о риске потери работы

## Правило коммитов по документации
- Если изменялся `main.py`, изменения в `docs/`/`README`/`INSTALLATION` коммить вместе с этим изменением в одном осмысленном коммите
- Не делай отдельные коммиты "только docs", если это сопроводительные изменения к правке кода
- Отдельный docs-only коммит допустим только по явному запросу пользователя
- Docs-изменения не описывай в тексте коммита: заголовок и тело формулируй только по ключевому изменению кода

## Стандарт сообщений коммитов
- Язык: русский
- **Заголовок начинается с типа** (Conventional Commits): `feat:` / `fix:` / `chore:` / `docs:` / `refactor:` / `test:`. Тип = вес коммита: feat-цикл и fix-цикл различимы с одного взгляда.
- После типа — конкретно что именно изменено в коде, без общих формулировок. Пример: `fix: опознавание безымянного TIFF по magic-байтам`
- Тело: 1-2 строки "зачем/эффект"
- Не используй расплывчатые сообщения (`обновлен конвейер`, `фикс`, `misc`, `wip`)

## Мини-чеклист
`git status` → `git pull` → `git add` → согласовать текст коммита с пользователем → `git commit` → `git push` во все настроенные remote, если Push был запрошен
